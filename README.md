# horse_quant_project (Bootstrap)

Bootstrap skeleton for a calibrated horse-racing ML pipeline.

## Tooling
- Python 3.11, Poetry
- black, ruff, mypy (**strict**), pytest, pre-commit

## Quickstart
```bash
poetry install
poetry run pre-commit install
make check   # or run the commands below individually on Windows
